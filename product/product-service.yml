apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: kiii-proekt
spec:
  selector:
    app: product-kiii-proekt
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer