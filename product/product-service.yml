apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: kiii
spec:
  selector:
    app: kiii-proekt
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
  type: LoadBalancer