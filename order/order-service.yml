apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: kiii-proekt
spec:
  selector:
    app: order-kiii-proekt
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer